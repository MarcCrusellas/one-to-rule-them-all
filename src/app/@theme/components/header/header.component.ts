import { Component } from '@angular/core';

@Component({
  selector: 'app-header',
  standalone: true,
  imports: [],
  templateUrl: './header.component.html',
  styleUrl: './header.component.scss'
})
export class HeaderComponent {
  a() {
    // const yes = ask('Are you sure?', { title: 'One to rule them all', type: 'info' });
    // const msg = message('File not found', { title: 'One to rule them all', type: 'error' })
    
    
  }

}
